import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sammytrsiregar.flipandmatch',
  appName: 'Flip and Match',
  webDir: 'dist'
};

export default config;
